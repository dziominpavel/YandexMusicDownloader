## 1. Каркас Go-модуля и config

- [x] 1.1 Создать Go-модуль и структуру `src/cmd/app`, `src/internal/{config,resolver,downloader,tagger}` — проверить `go build ./...` собирается
- [x] 1.2 Реализовать `config`: чтение `.env` рядом с exe (`YANDEX_TOKEN`, `OUTPUT_DIR`), валидация через `account/status` — проверить логом «токен ок / токен пуст» при старте

## 2. Resolver и downloader (порт Stmol)

- [x] 2.1 Портировать парсинг ссылки на трек (`source/url.go` из `references/01`) — проверить unit-тестом на 3–5 реальных URL
- [x] 2.2 Портировать MP3-путь (`download-info → max bitrate → XML → md5 → GET`, потоковая запись в temp) — проверить скачиванием 1 трека в MP3
- [x] 2.3 Портировать lossless-путь (`get-file-info → HMAC → AES-CTR → flac|flac-mp4`) + fallback на MP3 — проверить: трек с lossless даёт `.flac`, без — `.mp3` с пометкой fallback
- [x] 2.4 Атомарная публикация (`temp → tag → rename`), пропуск существующих, единый канал событий — проверить: обрыв сети не оставляет готового файла

## 3. Tagger

- [x] 3.1 Портировать теги MP3/FLAC/M4A по схеме из `docs/03` (мультиартист, totals, `YM_ID`, обложка 400px по умолчанию) — проверить чтением тегов скачанного файла в плеере
- [x] 3.2 Имена файлов по шаблону `Author/Album/NN - Title` с санитизацией — проверить скачиванием трека со «сложным» названием

## 4. Окно и exe

- [x] 4.1 Wails-скелет: окно с полем, кнопкой «Скачать», логом; бэкенд вызывает downloader, события рисуются в лог — проверить вводом ссылки вручную
- [x] 4.2 Сборка `app.exe` с `-H=windowsgui` (без консоли), запуск двойным кликом рядом с `.env` — проверить на чистой папке
- [x] 4.3 Сквозная проверка 5–10 треков (FLAC есть / нет / уже скачан / битая ссылка) — сверить лог и теги с `openspec/specs/*/spec.md`

## 5. Инструкция для друзей (черновик)

- [x] 5.1 Написать `docs/friends-setup.md`: где взять токен, куда положить `.env`, как запустить exe — проверить чтением «с нуля» вторым человеком
