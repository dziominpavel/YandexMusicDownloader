## 1. Resolver плейлистов

- [x] 1.1 Добавить парсинг `playlists/{uuid}` (префиксы `xx.`, query-хвосты, хосты ru/com/kz/by/uz) и проверить `go test ./internal/resolver/ -run TestParse -v` проходит
- [x] 1.2 Добавить резолв `GET /playlist/{uuid}` в список треков с маппингом в `Track` и проверить на реальном uuid руками: возвращается непустой список с названиями
- [x] 1.3 Проверить пагинацию больших плейлистов (100+ треков, поле `Pager`) и зафиксировать итог: хватает одного GET или нужны страницы, проверка — лог резолва плейлиста 100+ показывает все треки

## 2. Пакетное скачивание и прогресс

- [x] 2.1 Добавить событие `Progress{TrackID, Label, Done, Total, Percent}` и счетчик байт в обеих качалках (`downloadToTemp`, `streamLosslessTemp`) с троттлингом ~200мс/+1%, проверка — `go test ./internal/downloader/ -v` проходит
- [x] 2.2 Реализовать worker pool на 3 (семафор, дедуп по ID до запуска, skip недоступных, продолжение при ошибке, сбор `ok/skip/error`) и проверить unit-тестом на стабах: 5 треков с дублем, недоступным и ошибкой дают верный итог
- [x] 2.3 Сохранить для каждого трека пачки стандартный пайплайн (FLAC first, MP3 fallback, теги, `temp → rename`) без регрессии одиночного трека, проверка — `go test ./...` проходит и одиночный трек качается как раньше

## 3. UI и фронт

- [x] 3.1 Добавить блокирующий `DownloadPlaylist(link)` (заголовок `[playlist]`, проброс `log`+`progress`, финал `[finished]`), кнопка disabled на время пачки, проверка — вставка playlist-ссылки в окно качает все треки и показывает итог
- [x] 3.2 Добавить во фронт строки прогресса с ключом по trackID (обновление на месте, отдельный канал `progress`, канал `log` без изменений), проверка — при пачке 3 треков видны 3 независимо обновляющиеся строки процентов
- [x] 3.3 Автоскрытие бара завершенного трека (100% или терминальная строка `[done]/[skip]/[error]`, остатки сносит `[finished]`), проверка — `node --check` скрипта проходит, после `[done]` строк прогресса не остается
- [x] 3.4 Одна строка на трек: `Event.TrackID`, каркас `tracks` + канал `track-status`, статусы и бар обновляются на месте, в логе только заголовок и `[finished]`, проверка — `go test ./...` и `node --check` зеленые
- [x] 3.5 Настраиваемое число воркеров `PLAYLIST_WORKERS` (1..10, дефолт 3): парсинг в `config`, поле `Client`, проброс в UI, пример в `.env.example`, проверка — `go test ./...` проходит, включая новые кейсы парсинга и лимита

## 4. Сквозная проверка

- [x] 4.1 Прогнать вручную плейлист 5–10 треков (FLAC, fallback, skip существующего, недоступный) и проверить лог: заголовок, прогресс по каждому, `[done]/[skip]/[error]`, финал сходится с файлами в `downloads/`
- [x] 4.2 Запустить `openspec validate --all` и `go test ./...`, убедиться что спеки и тесты зеленые
